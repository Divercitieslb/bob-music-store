# The Shopify GitHub integration syncs this whole branch as the theme and
# enforces a 50MB limit, so everything that is not a theme file lives on the
# 'catalogue' branch instead: product images, the import CSVs, the build
# scripts and the static design previews.
import/
_work/
scripts/
preview-*.html

# local preview server config
.claude/

.DS_Store
Thumbs.db
